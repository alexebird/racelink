export default class CutTracker {
  constructor() {
    // TODO write cut_id and transcription result to line-wise json file.
    // open file, append compact json line, close file.
    // write in the mission transcripts dir.
    // use worldEditorCppApi.generateUUID() for ids
    //
    // then merge all recorded data togther to get the transcript and snaproad.
  }
}
